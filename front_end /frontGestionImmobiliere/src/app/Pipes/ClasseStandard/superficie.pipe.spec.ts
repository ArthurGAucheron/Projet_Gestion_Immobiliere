import { SuperficiePipe } from './superficie.pipe';

describe('SuperficiePipe', () => {
  it('create an instance', () => {
    const pipe = new SuperficiePipe();
    expect(pipe).toBeTruthy();
  });
});
