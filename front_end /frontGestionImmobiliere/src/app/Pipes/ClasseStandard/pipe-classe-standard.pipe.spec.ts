import { PipeClasseStandardPipe } from './pipe-classe-standard.pipe';

describe('PipeClasseStandardPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeClasseStandardPipe();
    expect(pipe).toBeTruthy();
  });
});
